const http = require('http');function makeRequest(path, method, data) {  return new Promise((resolve, reject) => {    const postData = data ? JSON.stringify(data) : '';    const options = {      hostname: 'localhost',      port: 5000,      path: `/api${path}`,      method: method,      headers: {        'Content-Type': 'application/json',        'Content-Length': Buffer.byteLength(postData)      }    };    const req = http.request(options, (res) => {      let responseData = '';      res.on('data', (chunk) => {        responseData += chunk;      });      res.on('end', () => {        try {          const parsed = JSON.parse(responseData);          resolve({ status: res.statusCode, data: parsed });        } catch (e) {          resolve({ status: res.statusCode, data: responseData });        }      });    });    req.on('error', (error) => {      reject(error);    });    if (postData) {      req.write(postData);    }    req.end();  });}async function testSignup() {  console.log('🧪 Testing Signup API with new email...');  const email = `test${Date.now()}@example.com`;  const username = email.split('@')[0];  console.log(' Email:', email);  console.log(' Username:', username);  try {    const result = await makeRequest('/taikhoan', 'POST', {      username: username,      password: '123456',      email: email,      hoTen: username    });    console.log(' Status:', result.status);    console.log(' Response:', result.data);    if (result.status === 201) {      console.log(' Signup successful!');      return { email, username };    } else {      console.log(' Signup failed!');      return null;    }  } catch (error) {    console.error(' Error:', error.message);    return null;  }}async function testLogin(email) {  console.log('\n🧪 Testing Login API...');  try {    const result = await makeRequest('/taikhoan/login', 'POST', {      email: email,      password: '123456'    });    console.log(' Status:', result.status);    console.log(' Response:', result.data);    if (result.status === 200) {      console.log(' Login successful!');    } else {      console.log(' Login failed!');    }  } catch (error) {    console.error(' Error:', error.message);  }}async function runTests() {  console.log(' Starting API tests with new email...\n');  const signupResult = await testSignup();  if (signupResult) {    await testLogin(signupResult.email);  }  console.log('\n Tests completed!');}runTests();